function largestNumFinder(numOne, numTwo, numThree) {
    const largest = Math.max(numOne,numTwo,numThree);
    console.log(`The largest number is ${largest}.`);
}

largestNumFinder(5, -3, 16);

largestNumFinder(-3, -5, -22.5);